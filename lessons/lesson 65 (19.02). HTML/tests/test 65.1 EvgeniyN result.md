Вопросы с выбором варианта: 78/78%  
# Вопрос 1. Блок 1. Предварительная подготовка
  
### Что нужно сделать до всех заданий

1. Создай папку проекта.
2. Создай файлы:
   - `index.html`
   - `styles.css`
3. Добавь базовую структуру HTML (каркас документа).
4. Подключи CSS через `link`.
5. Выбери тему страницы. Это будет “страница-визитка” с секциями и карточками.

### Правила (обязательные)

- Стили пишем только в `styles.css`.
- В HTML используем семантические теги там, где это уместно.
- Имена классов пишем латиницей.
- В конце занятия должна получиться одна цельная страница, а не набор кусочков.

### Как правильно пользоваться ИИ (чтобы это было обучение)

- Не отправляй в ИИ “сделай красиво”. Отправляй конкретные условия.
- Не копируй результат вслепую. Сначала прочитай и пойми структуру.
- После ИИ обязательно внести изменения по доп. условиям. Это часть сдачи.

---

## Блок 2. Каркас HTML и семантика

### Содержание и вопросы

- Что такое базовая структура HTML-документа?
- Зачем нужны `head` и `body`?
- Какие семантические теги подходят для шапки, основного контента и подвала?
- Как правильно использовать заголовки `h1`-`h3`?

### Материал (лаконично)

- `<!doctype html>` это “режим HTML”. Без него браузер может странно интерпретировать разметку.
- `head` это “настройки страницы”:
  - кодировка, чтобы текст на русском не ломался
  - `viewport`, чтобы страница нормально выглядела на телефоне
  - `title`, чтобы было понятное имя вкладки
  - подключение `styles.css`
- `body` это все, что видит пользователь.
- Семантические теги это понятные названия блоков вместо безликих контейнеров. Они помогают:
  - быстрее ориентироваться в коде
  - лучше понимать структуру страницы
  - подготовить основу для доступности
- Минимальный набор для страницы-визитки: `header` (шапка), `main` (основное), `footer` (подвал).
- Заголовки:
  - `h1` один на страницу, это главный смысл страницы
  - `h2` для секций
  - `h3` для элементов внутри секции, например для карточек
  
Варианты ответов:
1) ✅ Прочитано
  
### ИИ анализ
Этот вопрос проверяет ознакомление с инструкцией. Отметка "Прочитано" означает, что студент подтвердил изучение материала. Важно не просто отметить пункт, но и в дальнейшем применять эти правила на практике, особенно касающиеся семантики и структуры документа.  

---

#  Вопрос 2. Практическое задание (Блок 2)
  
### Условия

Сделай каркас `index.html` пошагово:

1. Создай базовую структуру документа.
2. В теге `html` установи язык документа.
3. В `head` добавь:
   - кодировку
   - настройку `viewport`
   - заголовок вкладки
   - подключение `styles.css`
4. В `body` создай 3 блока:
   - шапка
   - основная часть
   - подвал
5. В шапке добавь:
   - главный заголовок страницы
   - короткую строку-описание
6. В основной части создай минимум 3 секции.
7. В каждой секции добавь заголовок.

Проверка:

- Главный заголовок один.
- Секций в основной части не меньше трех.

### Псевдокод решения

```text
CREATE HTML_DOCUMENT
  SET DOCUMENT_LANGUAGE
  ADD META_CHARSET
  ADD META_VIEWPORT
  SET PAGE_TITLE
  LINK STYLESHEET_FILE

  BODY
    HEADER
      MAIN_TITLE
      SUBTITLE

    MAIN
      SECTION_A
        SECTION_TITLE
        PARAGRAPH

      SECTION_B
        SECTION_TITLE
        CONTENT_PLACEHOLDER

      SECTION_C
        SECTION_TITLE
        CONTENT_PLACEHOLDER

    FOOTER
      CONTACTS_PLACEHOLDER
END
```

### Если использовал ИИ, обязательные изменения вручную

- Переименуй минимум 2 семантических блока (например, добавь `nav` или обертку-контейнер), сохранив смысл.
- Проверь, что заголовки идут по уровням без пропусков.

---

**Вставь решение в комментарий ниже:**
  
### 🟢 Ответ
<!DOCTYPE html>  
<html lang="ru">  
<head>  
<meta charset="UTF-8">  
<meta name="viewport" content="width=device-width, initial-scale=1.0">  
<title>Это просто тестовый сайт</title>  
<link rel="stylesheet" href="styles.css">  
</head>  
<body>  
	 <div class="new-style">  
  
		<header>  
			<h1>Главный заголовок этой страницы<h1>  
			<p>Сайт показывает этапы работы с версткой<p>  
			<nav>  
                <a href="#about">О себе</a> |  
				<a href="#goals">Цели</a> |  
				<a href="#hobby">Увлечения</a>  
            </nav>  
		</header>  
		<main>  
			<!-- Секция 1 -->  
			<section id="about">  
				<h2>О себе</h2>  
				<p>Меня зовут Евгений, я осваиваю веб-разработку.</p>  
			</section>  
  
			<!-- Секция 2 -->  
			<section id="goals">  
				<h2>Мои цели</h2>  
				<p>Научиться создавать красивые и современные сайты.</p>  
			</section>  
  
			<!-- Секция 3 -->  
			<section id="hobby">  
				<h2>Мои увлечения</h2>  
				<p>Увлекаюсь изготовлением кожанных изделий из натуральной кожи</p>  
			</section>  
			 <p>  
				Ссылка: <a href="https://gmail.com" target="_blank">Открыть сайт</a>  
			</p>  
  
			<img  
			src="https://photofile.ru/wp-content/uploads/2024/04/1684700436047.png"  
			alt="Пример картинки"  
			width="778"  
			height="780"  
			/>  
		</main>  
	 </div>  
  
    <!-- ПОДВАЛ -->  
	<footer>  
		<p>Контакт для связи: myemail@gmail.com</p>  
		<p>2026, Все права защищены.</p>  
	</footer>  
  
</body>  
</html>  
  
### ИИ анализ
Отличная работа! Базовая структура документа создана корректно: указан `DOCTYPE`, язык, все необходимые мета-теги и подключение CSS. Семантические блоки `header`, `main`, `footer` использованы правильно, а также добавлен `nav` в шапку, что соответствует требованию по ручному изменению. Заголовки идут по уровням (`h1` в `header`, `h2` в секциях). Есть небольшая опечатка: тег `<h1>` не закрыт в строке 12 (нет `</h1>`), и аналогично для `<p>` в строке 13. Исправление этих ошибок сделает код валидным.  

---

# Вопрос 3. Где находится весь контент, который видит пользователь на странице (текст, картинки, ссылки)?
  
  
Варианты ответов:
1) ✅ Внутри `body`
2) Внутри `link`
3) Внутри `title`
4) Внутри `head`
5) Внутри `meta`
  
### ИИ анализ
Правильный ответ — внутри `body`. Тег `body` содержит всё видимое содержимое веб-страницы: текст, изображения, ссылки, видео и другие элементы. Остальные варианты неверны: `head` содержит метаданные и настройки, `title` — заголовок вкладки, `link` — подключение внешних ресурсов, а `meta` — служебную информацию.  

---

# Вопрос 4. Для чего нужен мета-тег `viewport`, чтобы страница нормально выглядела на мобильных телефонах?
  
  
Варианты ответов:
1) Для создания списка
2) ✅ Для нормального вида на телефоне
3) Для настройки шрифтов
4) Для вставки картинки
5) Для включения JavaScript
  
### ИИ анализ
Мета-тег `viewport` управляет масштабированием и размерами области просмотра на мобильных устройствах. Без него браузер может отображать страницу как уменьшенную десктопную версию, что ухудшает читаемость. Правильная настройка `viewport` (как в ответе студента: `width=device-width, initial-scale=1.0`) обеспечивает адаптивное отображение контента.  

---

# Вопрос 5. Какие семантические теги лучше использовать для создания основной структуры страницы-визитки (шапка, основной контент, подвал)?
  
  
Варианты ответов:
1) `table`, `tr`, `td`
2) `script`, `style`, `link`
3) `b`, `i`, `u`
4) ✅ `header`, `main`, `footer`
5) `form`, `input`, `button`
  
### ИИ анализ
Для семантической разметки страницы-визитки правильно использовать `header` (шапка), `main` (основное содержимое) и `footer` (подвал). Эти теги точно описывают назначение блоков, улучшая читаемость кода и доступность. Остальные варианты предназначены для других целей: таблиц, скриптов, стилей, форматирования текста или создания форм.  

---

# Вопрос 6. Как правильно использовать заголовок `h1` на странице?
  
  
Варианты ответов:
1) Использовать в каждой секции
2) Использовать вместо текста в `p`
3) ✅ Использовать один раз для всей страницы
4) Не использовать никогда
5) Использовать только в `footer`
  
### ИИ анализ
Заголовок `h1` — это главный заголовок всей страницы, который определяет её основную тему. Согласно лучшим практикам и требованиям доступности, на одной странице должен быть только один `h1`. Это помогает поисковым системам и скринридерам понять структуру контента. Использование нескольких `h1` размывает семантическую иерархию.  

---

# Вопрос 7. Почему семантические теги (например, `header`, `main`, `footer`) лучше обычных `div` для структуры страницы?
  
  
Варианты ответов:
1) Чтобы скрыть контент
2) Чтобы отключить отступы
3) Чтобы ускорить интернет
4) Чтобы заменить CSS
5) ✅ Чтобы структура была понятнее
  
### ИИ анализ
Семантические теги делают структуру HTML-документа понятнее для разработчиков, браузеров, поисковых систем и вспомогательных технологий (например, скринридеров). В отличие от универсального `div`, они несут смысловую нагрузку, указывая на роль блока (шапка, основное содержание, подвал). Это улучшает доступность и SEO, но не влияет напрямую на скорость или стилизацию.  

---

# Вопрос 8. В каком месте HTML-документа правильно подключать CSS-файл через тег `link`?
  
  
Варианты ответов:
1) В `body` после `footer`
2) Внутри `title` страницы
3) Внутри `meta charset`
4) Внутри комментария
5) ✅ В `head` через `link`
  
### ИИ анализ
Тег `<link>` для подключения внешней таблицы стилей (CSS) должен располагаться внутри раздела `<head>` HTML-документа. Это гарантирует, что стили будут загружены и применены до отображения содержимого `body`, что предотвращает мерцание нестилизованного контента. Размещение в `body` или других местах не соответствует стандартам и может привести к проблемам с загрузкой.  

---

# Вопрос 9. Зачем нужно добавлять атрибут `lang` (например, `lang="ru"`) в открывающем теге `html`?
  
  
Варианты ответов:
1) Чтобы включить flexbox
2) ✅ Чтобы указать язык документа
3) Чтобы поменять цвет фона
4) Чтобы увеличить заголовки
5) Чтобы отключить ссылки
  
### ИИ анализ
Атрибут `lang` указывает основной язык содержимого веб-страницы (например, `"ru"` для русского). Это важно для доступности: скринридеры выбирают правильное произношение. Это также помогает поисковым системам корректно индексировать страницу и браузерам предлагать перевод. Он не влияет на стили или поведение элементов, такие как Flexbox или цвета.  

---
