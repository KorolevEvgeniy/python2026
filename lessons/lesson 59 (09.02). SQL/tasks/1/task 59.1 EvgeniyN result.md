Время затраченное на выполнение: 0:38

result: 100/100

1) **Сильные стороны**
- Все запросы соответствуют условиям задания, правильно используют синтаксис SQL.
- Запросы компактные, без лишнего кода, легко читаются.
- Правильно используются операторы сравнения и логические операторы (WHERE, AND, !=).
- Учтены все три уровня заданий: базовый, средний и продвинутый.
- Имена столбцов соответствуют ожидаемым (например, Survived=1 для выживших, Sex='female' для женщин).

2) **Ошибки и недочёты (сгруппировать по серьёзности)**

**Блокирующие (ломает выполнение требований задания)**
- Отсутствуют.

**Значимые (может дать неверный результат на части кейсов, сильно ухудшает качество)**
- Отсутствуют.

**Минорные (стиль, читаемость, мелкие улучшения без влияния на правильность)**
- В первом запросе (`SELECT * from titanic;`) и восьмом (`SELECT * from titanic WHERE Survived=0 and SibSp=0 and Parch=0;`) используется `*` для выбора всех столбцов, хотя в условии задачи 1 сказано "извлечь все данные о пассажирах", а в задаче 2 уровня 3 — "выведите список пассажиров". Это соответствует требованию, но для улучшения читаемости можно было бы явно перечислить столбцы, особенно в задаче 2 уровня 3, где требуется "список пассажиров" (подразумевается, вероятно, все данные). Однако это не ошибка, так как условие допускает такую интерпретацию.
- В некоторых запросах отсутствует сортировка, если она не требуется явно (например, в задаче 2 уровня 2 "Выведите имена пассажиров, которые путешествовали одни" сортировка не указана, и студент её не добавил — это корректно).
- Стиль написания SQL (нижний регистр для ключевых слов, отсутствие точек с запятой в конце каждого запроса, кроме последнего) — это вопрос предпочтений, но для улучшения согласованности можно рекомендовать использовать верхний регистр для ключевых слов SQL и завершать каждый запрос точкой с запятой.

3) **Оценка и как она посчитана**
- Функциональность и соответствие условию: 50/50 (все запросы корректны, соответствуют условиям, включая фильтрацию, сортировку и выбор столбцов).
- Качество кода (структура, читаемость, устойчивость, отсутствие дублирования): 30/30 (код структурирован по задачам, читаем, дублирования нет, устойчивость не требуется по условию).
- Стиль и тесты: 20/20 (тесты не требовались, стиль приемлем, хотя есть мелкие замечания, но они не влияют на оценку).

Итог: 100/100.

4) **Если задание выполнено не полностью**
- Задание выполнено полностью. Все три уровня реализованы, каждая задача решена корректно.
