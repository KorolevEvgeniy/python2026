Время затраченное на выполнение: 0:38

result: 60/100

1) **Сильные стороны**
- Созданы все три требуемые таблицы с правильными полями.
- Вставлены записи в соответствии с примерами псевдокода.
- Выполнены операции UPDATE и DELETE для соответствующих уровней.
- Код структурирован по уровням задания.

2) **Ошибки и недочёты**

**Блокирующие (ломает выполнение требований задания)**
- Отсутствуют запросы SELECT для получения и отображения всех записей из таблиц. В условии каждого уровня явно указано: "Выполните запрос, чтобы получить все записи из таблицы и отобразить их" (уровень 1), "Выполните запрос, чтобы получить все записи и увидеть изменения" (уровень 2), "Выполните запрос, чтобы получить все оставшиеся товары" (уровень 3). В решении нет ни одного оператора SELECT. Это делает задание невыполненным по основной цели — получению и отображению данных.

**Значимые (может дать неверный результат на части кейсов, сильно ухудшает качество)**
- Использование кавычек в значениях для полей типа TEXT. Например, `'"Иван"'` — это строка, содержащая кавычки как часть данных (`"Иван"`), а не просто `Иван`. Это приведёт к выводу некорректных имён при отображении. Нужно вставлять значения без лишних кавычек: `'Иван'`.
- Для таблицы `prodagi` (товары) поле `quantity` (количество) имеет тип REAL (число с плавающей точкой), хотя количество товаров — целое число. Логичнее использовать INTEGER.
- Имя таблицы `prodagi` не соответствует условию ("товары"). Хотя это не критично для работы, но снижает читаемость. Лучше использовать `products` или `tovary`.
- Имя таблицы `sotrudniki` содержит опечатку (правильно `sotrudniki`? Имелось в виду `sotrudniki`? В любом случае, лучше `employees` или `sotrudniki` без ошибок).

**Минорные (стиль, читаемость, мелкие улучшения без влияния на правильность)**
- Использование DROP TABLE без проверки существования таблицы может вызвать ошибку при первом запуске, если таблицы ещё нет. Хотя в SQLite это допустимо (IF EXISTS не поддерживается в некоторых версиях), лучше добавить условие `DROP TABLE IF EXISTS`.
- Вставка значений с явным указанием id, хотя используется AUTOINCREMENT. Это может привести к конфликтам, если база уже содержит записи. Можно опустить id при вставке, чтобы база сама генерировала значения.
- Отсутствует закрытие соединения с базой данных (хотя в SQL-скрипте это не требуется явно, но в контексте задания, подразумевающего программу, это было бы важно).

3) **Оценка и как она посчитана**
- Функциональность и соответствие условию: 20/50 (минус 30 за отсутствие SELECT-запросов для отображения данных, что является ключевым требованием; минус 5 за некорректные кавычки в данных и тип REAL для количества, что ухудшает корректность вывода).
- Качество кода (структура, читаемость, устойчивость, отсутствие дублирования): 20/30 (минус 5 за потенциальные ошибки из-за DROP TABLE без проверки и явных id с AUTOINCREMENT; минус 5 за неидеальные имена таблиц и типы данных).
- Стиль и тесты: 20/20 (тесты не требовались, код написан без явных стилистических ошибок, кроме уже учтённых).

4) **Если задание выполнено не полностью**
- Отсутствует: выполнение SELECT-запросов для получения и отображения всех записей из каждой таблицы.
- Сделано частично: создание таблиц, вставка, обновление и удаление данных выполнены, но без финального вывода результатов.

**Вариант исправления (дополнение к коду студента):**
Добавить после операций INSERT/UPDATE/DELETE для каждой таблицы соответствующий SELECT:

```sql
-- Для уровня 1
SELECT * FROM students;

-- Для уровня 2
SELECT * FROM sotrudniki;

-- Для уровня 3
SELECT * FROM prodagi;
```

Также исправить вставку значений, убрав лишние кавычки, и изменить тип поля `quantity` на INTEGER:

```sql
-- Исправленная вставка для students
INSERT INTO students(name, age) VALUES ('Иван', 20), ('Мария', 22), ('Петр', 19);

-- Исправленная вставка для sotrudniki
INSERT INTO sotrudniki(name, position, salary) VALUES ('Анна', 'Разработчик', 50000), ('Олег', 'Менеджер', 60000), ('Елена', 'Дизайнер', 45000);

-- Исправленная таблица prodagi и вставка
DROP TABLE IF EXISTS products;
CREATE TABLE products(
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    price REAL,
    quantity INTEGER
);
INSERT INTO products (name, price, quantity) VALUES ('Ноутбук', 45000, 10), ('Мышь', 1500, 50), ('Клавиатура', 3000, 25), ('Монитор', 12000, 15);
DELETE FROM products WHERE id = 3;
SELECT * FROM products;
```
