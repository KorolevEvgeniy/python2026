result: 80/100

### 1. Формулировка задания
Написать функцию `delivery_price(city, weight_kg, urgent)`, которая:
- Возвращает `500 + 30 * weight_kg` при `urgent=True`
- Возвращает `300 + 20 * weight_kg` при `urgent=False`
- Параметр `city` пока не влияет на расчёты (зарезервирован для будущего).
Вызвать функцию 4 раза: дважды позиционно и дважды с именованными аргументами. Добавить закомментированный пример **намеренно неправильного** вызова, демонстрирующий ошибку (например, `TypeError` или `SyntaxError`).

---

### 2. Результаты проверки
**Файл:** `PR37_2.py`  
**Корректные части:**
- Функция `delivery_price` реализована верно (строки 1-6).
- Выполнено 4 вызова функции (строки 8-11):  
  - `pos1`, `pos2` — позиционные вызовы.  
  - `imen1`, `imen2` — именованные вызовы (включая вариацию порядка аргументов).  
- Все вызовы используют допустимые типы данных (ошибок при запуске нет).

**Проблемы:**
- **Отсутствует закомментированный пример неправильного вызова** (требование п.3 задания).  
- Лишний вызов функции в `print` (строка 7) — всего 5 вызовов вместо требуемых 4 (но это не нарушает логику).

---

### 3. Сильные стороны
- **Корректность**: Функция правильно вычисляет стоимость доставки для обоих случаев (`urgent`/не `urgent`).
- **Читаемость**: Имена переменных и параметров понятны (`weight_kg`, `urgent`).
- **Архитектура**: Использованы оба типа вызовов (позиционные и именованные), включая разный порядок аргументов в именованных вызовах.

---

### 4. Ошибки
**Значимые (снижение на 20 баллов):**
1. **Не реализован п.3 задания** (демонстрация ошибки):  
   *Как исправить:* Добавить закомментированный пример с ошибкой, например:  
   ```python
   # delivery_price("Rim", True, 20)  # TypeError: unsupported operand type(s) for *: 'int' and 'bool'
   ```

---

### 5. Оценка
- **Функциональность (50%): 40/50**  
  Основная логика работает, но не выполнено требование по демонстрации ошибки (-10).
- **Качество кода (30%): 30/30**  
  Нет синтаксических ошибок, код читаем.
- **Стиль и тесты (20%): 10/20**  
  Нет тестов (но они не требовались), отсутствует комментарий с ошибкой (-10).

**Итог: 80/100**  
Снижение только за отсутствие закомментированного примера с ошибкой.

---

### 6. Вариант исправления
Добавить в конец файла:  
```python
# Пример намеренно неправильного вызова:
# delivery_price("Rim", True, 20)  # TypeError: умножение int на bool (weight_kg=True)
```

---

Анализ выполнен моделью: GPT-4o
