result: 100/100

### 1. Краткое описание задания
Требовалось реализовать три функции:
1. `rectangle_info(width, height)` — возвращает кортеж `(площадь, периметр)` прямоугольника.
2. `is_adult(age)` — возвращает `True`, если возраст ≥ 18, иначе `False`.
3. `safe_div(a, b)` — возвращает `a / b`, но при `b = 0` возвращает `None`.

**Ограничения**: 
- Входные данные: числа (для всех функций).
- Ожидаемый вывод: кортеж для `rectangle_info`, булево значение для `is_adult`, число или `None` для `safe_div`.

---

### 2. Результаты проверки
**Тестирование выполнено**:
1. **rectangle_info**:
   - Проверка вызова `rectangle_info(13, 8)` → возвращает `(104, 42)`, что корректно.
2. **is_adult**:
   - Проверка `is_adult(18)` → `True`, `is_adult(17)` → `False` (тестировалось в коде студента).
3. **safe_div**:
   - Проверка `safe_div(2, 0)` → `None`, `safe_div(10, 2)` → `5.0`.

**Файл**: `PR37_1.py` (строки 1–22).  
**Воспроизведение**: запуск файла выводит результаты тестовых вызовов функций, что подтверждает их работоспособность.

---

### 3. Сильные стороны работы
- **Корректность**: все функции реализованы согласно условию.
- **Читаемость**: код структурирован, есть пробелы между функциями.
- **Обработка крайних случаев**: учтено деление на ноль в `safe_div`.
- **Тестовые вызовы**: студент добавил проверки работы функций (хотя это не требовалось).

---

### 4. Ошибки и рекомендации
**Ошибок не обнаружено**.  
*Мелкие замечания (не влияют на оценку)*:
- **Избыточные тестовые вызовы**: вывод результатов через `print` не требовался в задании, но не нарушает функциональность.
- **Локализация переменных**: имена `ploshad`, `perimetr` на русском языке могут снижать читаемость в международных проектах. Рекомендуется использовать английские названия (например, `area`, `perimeter`).

---

### 5. Оценка
**Функциональность**: 50/50 — все задачи решены верно.  
**Качество кода**: 30/30 — код чистый, нет логических ошибок.  
**Стиль и тесты**: 20/20 — несмотря на избыточные `print`, баллы не снимаются, так как условие не запрещало их использование.  

**Итог**: 100/100.  
*Замечания не являются ошибками и не приводят к снижению балла.*

---

Анализ выполнен моделью: GPT-4o.
