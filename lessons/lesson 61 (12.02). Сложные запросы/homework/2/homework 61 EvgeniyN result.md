Дней на выполнение: 7

result: 0/100

1) **Сильные стороны**
- Студент предоставил файл базы данных, что соответствует части требований по созданию БД и таблицы.
- Файл имеет корректный формат SQLite 3, что говорит о технической возможности работы с базой данных.

2) **Ошибки и недочёты**

**Блокирующие (ломает выполнение требований задания)**
- **Отсутствует SQL-код создания таблицы и вложенного запроса.** В условии требуется явно описать таблицу `Students` с указанными столбцами и ограничениями, добавить записи и сформулировать вложенный запрос. Предоставлен только бинарный файл базы данных (`stubents.db`). Без SQL-кода невозможно проверить корректность структуры таблицы (поля, типы, ограничения), корректность добавленных данных и, самое главное, корректность вложенного запроса. Это прямое нарушение требований задания.
- **Название файла содержит опечатку (`stubents.db` вместо `students.db`).** Хотя это не влияет на функциональность, в контексте задания, где требуется создать таблицу `Students`, такая опечатка может указывать на невнимательность, но основная проблема — отсутствие кода.

**Значимые (может дать неверный результат на части кейсов, сильно ухудшает качество)**
- **Невозможно проверить содержимое базы данных.** Без приложенного SQL-скрипта или дампа данных нельзя убедиться, что таблица содержит необходимые поля (`id`, `name`, `birth_date`, `email`, `course_name`, `enrollment_date`), что данные добавлены корректно и что среди них есть курс «Программирование». Следовательно, нельзя проверить, будет ли вложенный запрос работать как задумано.

**Минорные (стиль, читаемость, мелкие улучшения без влияния на правильность)**
- Отсутствуют комментарии или пояснения в предоставленном материале (хотя это и не требовалось явно, но улучшило бы читаемость решения).

3) **Оценка и как она посчитана**
- **Функциональность и соответствие условию: 0/50.** Не предоставлен ключевой элемент задания — SQL-код для создания таблицы, заполнения данными и выполнения вложенного запроса. Предоставлен только итоговый файл БД без возможности проверить его соответствие требованиям.
- **Качество кода (структура, читаемость, устойчивость, отсутствие дублирования): 0/30.** Код отсутствует, оценивать нечего.
- **Стиль и тесты: 0/20.** Отсутствуют какие-либо проверки или пояснения, тесты не требовались, но и стиль кода оценить невозможно.
- **Итог: 0/100.** Задание не выполнено в требуемом формате.

4) **Если задание выполнено не полностью**
- **Отсутствует:** SQL-скрипт с командами `CREATE TABLE`, `INSERT` и `SELECT` (вложенный запрос).
- **Выполнено частично:** Создан файл базы данных, но его содержимое и способ создания не представлены для проверки.

**Вариант полного решения (SQL-скрипт):**

```sql
-- 1. Создание таблицы Students с требуемыми полями и ограничениями
CREATE TABLE IF NOT EXISTS Students (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    birth_date DATE,
    email TEXT UNIQUE NOT NULL,
    course_name TEXT NOT NULL,
    enrollment_date DATE NOT NULL
);

-- 2. Добавление тестовых данных (включая курс "Программирование")
INSERT INTO Students (name, birth_date, email, course_name, enrollment_date) VALUES
('Иван Иванов', '2000-05-15', 'ivan@example.com', 'Программирование', '2023-09-01'),
('Мария Петрова', '1999-11-22', 'maria@example.com', 'Математика', '2023-09-01'),
('Алексей Сидоров', '2001-03-30', 'alexey@example.com', 'Программирование', '2023-09-15'),
('Елена Кузнецова', '2000-08-10', 'elena@example.com', 'Физика', '2023-09-10');

-- 3. Вложенный запрос для выбора студентов, записанных на курс "Программирование"
SELECT name
FROM Students
WHERE email IN (
    SELECT email
    FROM Students
    WHERE course_name = 'Программирование'
);
-- Альтернативный, более простой запрос (без вложенности, но явно не нарушает условие):
-- SELECT name FROM Students WHERE course_name = 'Программирование';
```

**Рекомендуемые тест-кейсы для проверки:**
1. Запустить скрипт создания таблицы и убедиться, что таблица `Students` создаётся без ошибок.
2. Проверить структуру таблицы (например, через `.schema Students` в SQLite) на соответствие требованиям (поля `id`, `name`, `birth_date`, `email`, `course_name`, `enrollment_date`).
3. Выполнить запрос `SELECT * FROM Students;` и убедиться, что добавлено не менее 4 записей, включая курс «Программирование».
4. Выполнить вложенный запрос и убедиться, что он возвращает только имена студентов с `course_name = 'Программирование'` (в примере: 'Иван Иванов', 'Алексей Сидоров').
5. Проверить, что запрос не возвращает студентов с другими курсами (например, 'Мария Петрова').
